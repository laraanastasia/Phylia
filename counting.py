from discord import Intents, Client,Message
from discord.ext import commands,tasks
import discord

bot = commands.Bot(command_prefix="!",intents = discord.Intents.all())

#casync def startcounting():
    
